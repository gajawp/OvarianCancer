# transunet official integration

This directory is intentionally documentation-only. Add the authors' official repository code and released pretrained checkpoint here, then adapt only the dataset interface, number of output classes, checkpoint loading, and output resizing.

Do not label a simplified custom reimplementation as the official baseline. Record the exact repository commit, checkpoint name, backbone initialization, and any changes in the paper.
